# generated board renders are published to github pages, not committed
boards/**/renders/

# kicad editor lock files
*.lck

# kicad local history/backup dirs (were accidentally tracked as submodules)
.history/
.history_trim/
